# firecrawl-agent 环境配置
# 复制此文件为 .env 并填入实际值
# USE_SELF_HOST=true  → 使用自托管 Firecrawl（跳过云端 key 鉴权）
# USE_SELF_HOST=false → 使用 Firecrawl Cloud（keyless 免费档 / 已存全局凭证）
USE_SELF_HOST=true

# 自托管地址（USE_SELF_HOST=true 时生效）
FIRECRAWL_SELF_HOST_URL=<your-self-host-url>
