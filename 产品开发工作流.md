# 产品开发工作流

## 流程图

```mermaid
flowchart TB
    subgraph phase1[" 立项阶段 "]
        A["👥 团队立项"]
    end

    subgraph phase2[" 产品规划 — 产品经理 "]
        B["🔍 市场调研 + 竞品调研"]
        C["💡 产品形态定义"]
        D["📊 阶段划分"]
        E["🎯 本阶段目标确定"]
        F["📝 原型图 + 需求文档"]
        B --> C --> D --> E --> F
    end

    subgraph phase3[" 团队审核 "]
        G{审核是否通过？}
    end

    subgraph phase4[" 项目实施 — 组长：进度管控 + 质量控制 "]
        direction LR
        subgraph ui["  UI 设计  "]
            H["🎨 UI 效果图设计"]
        end
        subgraph fe["  前端开发  "]
            I["💻 面向用户页面开发"]
        end
        subgraph be["  后端开发  "]
            J["⚙️ 业务数据逻辑处理"]
        end
    end

    subgraph phase5[" 测试验证 — 组长：质量把关 "]
        K["🧪 功能测试与验收"]
    end

    subgraph phase6[" 上线发布 "]
        L["👥 组长 + 产品验收"]
        M["🚀 上线发布"]
        L -->|验收通过| M
    end

    A --> B
    F --> G
    G -->|"通过，并行推进"| H
    G -->|"通过，并行推进"| I
    G -->|"通过，并行推进"| J
    G -.->|"不通过，打回修改"| B
    H --> K
    I --> K
    J --> K
    K --> L

    classDef product fill:#4A90D9,color:#fff,stroke:#2E6AB0
    classDef ui fill:#27AE60,color:#fff,stroke:#1E8449
    classDef fe fill:#8E44AD,color:#fff,stroke:#6C3483
    classDef be fill:#E67E22,color:#fff,stroke:#BA4A00
    classDef test fill:#16A085,color:#fff,stroke:#117A65
    classDef approval fill:#D35400,color:#fff,stroke:#A04000
    classDef decision fill:#F39C12,color:#fff,stroke:#C87D0A

    class A,B,C,D,E,F product
    class H ui
    class I fe
    class J be
    class K test
    class L,M approval
    class G decision
```

## 角色与职责

| 角色 | 核心职责 |
|------|---------|
| **团队** | 立项发起、审核把关产品方向和需求 |
| **产品经理** | 市场调研 + 竞品调研 → 产品形态定义 → 阶段划分 → 本阶段目标 → 原型图 + 需求文档 |
| **UI 设计** | 根据原型图设计最终 UI 效果图 |
| **前端开发** | 开发面向用户的交互页面 |
| **后端开发** | 处理业务逻辑与数据层 |
| **测试** | 功能测试、缺陷跟踪、验收验证 |
| **组长** | 全流程进度管控、各成员产出质量控制、上线最终审批 |

## 关键规则

1. **产品出原型 + 文档后必须团队审核**，通过方可进入实施
2. **审核不通过打回产品**，重新调研与修改
3. **UI / 前端 / 后端审核通过后并行启动**，互不阻塞
4. **所有开发完成后统一进入测试**，避免边开发边测的混乱
5. **上线前组长 + 产品联合验收**，确保交付质量
